#include"1_2.h"
Gtwidget* MapSize(int length=10,int windth=10)
{
	GtkWidget *MainMap;
	MainMap=gtk_drawing_area_new();
	gtk_widget_set_size_request(MainMap,length,windth);
	gtk_container_add(GTK_CONTAINER(frame))
}
